
import { Code } from 'astro-expressive-code/components'
import { Quote } from '@flexiui/svelte-quote';
import PreviewCard from '../../../components/PreviewCard.svelte';

import Alert from '../../../components/ui/Alert.svelte';

import ComponentPreview from '../../../components/ComponentPreview.svelte';
import QuoteBasicExample from './examples/QuoteBasicExample.svelte';
import QuoteBasicExampleRaw from './examples/QuoteBasicExample.svelte?raw';

import QuoteEditableExample from './examples/QuoteEditableExample.svelte';
import QuoteEditableExampleRaw from './examples/QuoteEditableExample.svelte?raw';

<PreviewCard rounded outlined>
  <QuoteBasicExample client:load />
</PreviewCard>

<Alert client:load severity="warning">
  <span>This library is currently in <strong>fase beta</strong>. It’s <strong>not recommended</strong> for use <strong>in production</strong> environments.</span>
</Alert>

## Installation

To install the `Quote` component in your Svelte project, you can use npm or yarn.

```bash
npm install @flexiui/svelte-quote
```

## Basic Usage of the Component

Once installed, you can use the `Quote` component in your Svelte application.

<Code code={QuoteBasicExampleRaw} lang="svelte" />

## Editable Mode and Events

In editable mode, the component triggers a series of events that can be used to interact and respond to user actions. Additionally, using `bind:`, it is possible to link the component's content to a variable in the script, allowing automatic synchronization.

<div class="card preview rounded">
  <QuoteEditableExample />
</div>

<Code code={QuoteEditableExampleRaw} lang="svelte" />

### Available Events

- **`on:focus`**: Triggered when the component gains focus.
- **`on:keydown`**: Triggered when a key is pressed while the component is focused.
- **`on:input`**: Triggered whenever the user makes changes to the component's content.

### Bindings (`bind:`)

- **`bind:value`**: Allows synchronizing the editable content of the component with an external variable (in this case, `content`). Any changes to the component's content will update this variable, and any changes to the variable will update the component's content.
